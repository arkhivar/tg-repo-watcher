# File: .github/workflows/summarize.yml
name: Report to Telegram

on:
  push:
  pull_request:

jobs:
  report:
    uses: arkhivar/tg-repo-watcher/.github/workflows/summarize.yml@main
    secrets: inherit
